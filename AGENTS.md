# AGENTS.md

适用范围：整个仓库，除非子目录下另有 `AGENTS.md` 覆盖。

## 优先级
1. 系统 / 开发者 / 用户指令
2. 本文件
3. 现有代码风格、README 和局部约定

## 工作方式
- 开始任何任务前，先阅读相关代码和文档，不要依赖记忆直接改。
- 任务涉及 `.NET`、WPF、API、CLI、库或 SDK 时，优先使用 Context7 获取当前文档，不要用过时经验替代。
- 多步骤、跨文件或目标不清晰的任务，先建立计划并持续更新。
- 每次修改前，先简要说明准备做什么；修改后做最小必要验证。
- 不要回滚、覆盖或清理用户未要求处理的改动。

## 注释要求
- 方法、关键流程、关键分支、状态转换、外部调用和边界处理都要有注释说明。
- 公共类型和公共方法优先使用 XML 文档注释，写清楚用途、参数、返回值、副作用和失败条件。
- 非公开但较复杂的方法，至少在方法上方加一句目的说明；复杂代码块前补充它为什么存在、它依赖什么、它要保护什么。
- 注释不要只复述代码，要说明意图、约束、取舍和容易出错的地方。
- 注释要具体，避免空话；如果一段逻辑以后容易被改坏，就应该写清楚。
- 保持简洁但完整，代码变更后及时删除过时注释。
- 注释统一使用中文；只有在引用官方 API 名称、协议字段、异常类型或外部工具参数时保留原文。

## 代码结构要求
- 优先延续现有结构和局部模式，不要为了“更优雅”而重构整套架构。
- 保持 UI、服务、模型、持久化等边界清晰。
- 只有在能明显降低复杂度、重复或耦合时才拆分方法和类型。
- 新抽象必须对应真实问题，不要为抽象而抽象。
- 命名、文件组织、资源组织要克制、一致、可预测。

## 代码合理性要求
- 明确处理空值、异常、取消、释放和异步流程。
- 不要静默吞掉异常；如果失败是非致命的，也要给出状态反馈或可追踪原因。
- 不要阻塞 UI 线程。
- 网络、文件、外部进程、设置读取都要考虑失败和空状态。
- 避免硬编码环境路径、机器特定值和无说明的魔法数字。
- 改动后应保持可构建、可运行、可验证。

## 界面美观性要求
- 这是 Windows 桌面播放器，界面应克制、实用、信息密度合理。
- 避免营销页式布局、夸张渐变、无意义装饰和松散排版。
- 保持统一的间距、对齐、字号层级和控件尺寸。
- 主要操作应清晰可达，忙碌 / 禁用 / 空状态要明确。
- 文本不得溢出、遮挡或挤压关键控件；必要时换行或截断。
- 新界面优先满足“装好就能用”，不是只追求视觉效果。

## 任务规划要求
- 复杂任务先拆成可验证步骤，再动手。
- 任务开始时先按 `using-superpowers` 的流程思考和工作。
- 对超过几步或需要多次工具调用的任务，维护计划并及时更新状态。
- 不要把当前步骤的阻塞工作故意拖给未来。
- 若任务范围变大，先澄清边界，再继续扩展。

## 验证要求
- 能构建就构建，能运行就运行，至少做与改动最相关的验证。
- 如果无法验证，要明确说明原因，并给出建议命令或检查点。
